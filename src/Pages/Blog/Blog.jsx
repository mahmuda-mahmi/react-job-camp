import React from 'react';
import PropTypes from 'prop-types';

const Blog = () => {
    return (
        <div>
            <h2>Blog page</h2>
        </div>
    );
};

Blog.propTypes = {
    
};

export default Blog;